plugins {
    id "idea"
    id "eclipse"
}

group = "architecture"
version = "1.0.0"

asciidoctor {
    resources {
        from('src/resources/images') {
            include "*"
        }

        into "."
    }
}